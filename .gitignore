node_modules
dist
dist-ssr
*.local
.DS_Store
.vite
*.log

# Build info
*.tsbuildinfo

# Per-machine Claude Code config
.claude/settings.local.json
.claude/*.lock

# Large HDRI environment map — not versioned (see README "HDRI asset")
public/hdri/

# Stray nested skill scaffold (not part of the app)
your-project/

# Internal design docs — derived from confidential source material, and this
# repo is public. Keep them local; push deliberately if that ever changes.
TERRAGEN-PANEL.md
TERRAGEN-DISCUSSION.md
